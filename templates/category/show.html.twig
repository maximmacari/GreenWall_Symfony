{% extends 'base.html.twig' %}

{% block title %}Category
{% endblock %}

{% block body %}

	<div class="container mx-auto w-full lg:w-2/5 md:w-2/3">
		<table class="table">
			<tbody>
				<tr>
					<th>Category:
						{{ category.name }}</th>
				</tr>
				<tr>
					<th></th>
					<td></td>
				</tr>
			</tbody>
		</table>

		<a class="float-left btn btn-return" href="{{ path('category_index') }}">Back to list</a>

		<a class="float-right btn btn-accept" href="{{ path('category_edit', {'id': category.id}) }}">Edit</a>
        {{ include('category/_delete_form.html.twig') }}
	</div>
	
{% endblock %}
