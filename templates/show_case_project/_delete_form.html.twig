<form method="post" action="{{ path('show_case_project_delete', {'id': show_case_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ show_case_project.id) }}">
    <button class="btn">Delete</button>
</form>
