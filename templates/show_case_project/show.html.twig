{% extends 'base.html.twig' %}

{% block title %}ShowCaseProject{% endblock %}

{% block body %}
    <h1>ShowCaseProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ show_case_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ show_case_project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ show_case_project.description }}</td>
            </tr>
            <tr>
                <th>Images</th>
                <td>{{ show_case_project.images ? show_case_project.images|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('show_case_project_index') }}">back to list</a>

    <a href="{{ path('show_case_project_edit', {'id': show_case_project.id}) }}">edit</a>

    {{ include('show_case_project/_delete_form.html.twig') }}
{% endblock %}
